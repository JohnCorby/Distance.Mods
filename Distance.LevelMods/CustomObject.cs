using UnityEngine;

namespace Distance.LevelMods {
    /// just holds the data, to be found by the manager
    public class CustomObject : MonoBehaviour {
        public byte[] data = new byte[0];
    }
}
